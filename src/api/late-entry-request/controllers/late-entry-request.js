'use strict';

/**
 * late-entry-request controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::late-entry-request.late-entry-request');
